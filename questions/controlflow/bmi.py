# Calculate BMI and output. round to 2 decimal places

# Hint: prefer format-strings (f"...")
# BMI Formula:
#   BMI = weight / (height²), kg/m²

# According to the standard from somewhere, output the BMI type

weight = float(input(": "))
height = float(input(": "))

BMI = ...

if BMI < ...:
    pass
...
